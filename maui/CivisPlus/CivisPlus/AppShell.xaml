<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CivisPlus.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:CivisPlus"
    Shell.FlyoutBehavior="Disabled"
    Title="CivisPlus"
    
     Shell.BackgroundColor="{StaticResource Primary}"
    Shell.TitleColor="{StaticResource OnPrimary}"

    Shell.TabBarBackgroundColor="{StaticResource PageBackground}"
    Shell.TabBarTitleColor="{StaticResource Primary}"
    Shell.TabBarUnselectedColor="{StaticResource TextSecondary}">

    <TabBar>
        <Tab Title="Home">
            <Tab.Icon>
                <FileImageSource File="home_icon.svg"/>
            </Tab.Icon>
            <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
        </Tab>

        <Tab Title="Cidades">
            <Tab.Icon>
                <FileImageSource File="cidades_icon.svg"/>
            </Tab.Icon>
            <ShellContent
            ContentTemplate="{DataTemplate local:CidadesPage}"
            Route="CidadesPage" />
        </Tab>

        <Tab Title="Nome">
            <Tab.Icon>
                <FileImageSource File="nome_icon.svg"/>
            </Tab.Icon>
            <ShellContent
            ContentTemplate="{DataTemplate local:NomePage}"
            Route="NomePage" />
        </Tab>
    </TabBar>

</Shell>